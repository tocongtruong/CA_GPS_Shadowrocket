{
  "name": "ios-location-server",
  "version": "1.0.0",
  "description": "Backend server for receiving, storing (30-day retention), Telegram notifications and visualizing iOS location events from Shadowrocket MITM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "rotate-secrets": "node scripts/rotate-secrets.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^4.6.0",
    "telegraf": "^4.16.3"
  }
}
